#pragma once
#include <string_view>

namespace NewBase
{
    class DetourHook
    {
    private:
        /* data */
    public:
        DetourHook(/* args */);
        ~DetourHook() = default;
    };
}